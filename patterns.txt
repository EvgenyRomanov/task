/*
    Легковес.
    Flyweight.
    
    Легковес - структурный шаблон проектирования, 
    который позволяет сократить использование памяти приложением путем хранения "общего" состояния объектов только один раз, 
    вместо хранения этого состояния в каждом из создаваемых объектов.
*/


/*
    Фабричный метод.
    Factory Method.

    Также, кажется, есть попытка использовать фабричный метод,
    сутью которого явлется предоставить возможность потомкам класса каждому по своему реализовывать метод, 
    создающий какой-либо объект.
*/